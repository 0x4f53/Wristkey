<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <uses-feature android:name="android.hardware.type.watch" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Wristkey">
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name="app.wristkey.QRScannerActivity"
            android:label="@string/title_activity_bitwarden_jsonimport" />

        <activity
            android:name="app.wristkey.FileImportActivity"
            android:label="@string/title_activity_file_import" />

        <activity
            android:name="app.wristkey.ReceiveDecryptActivity"
            android:label="@string/title_activity_receive_decrypt" />

        <activity
            android:name="app.wristkey.EncryptSendActivity"
            android:label="@string/title_activity_encrypt_send" />

        <activity
            android:name="app.wristkey.MainActivity"
            android:label="@string/app_name"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name="app.wristkey.QRCodeActivity"
            android:label="QR code" />
        <activity
            android:name="app.wristkey.AddActivity"
            android:label="Add account" />

        <activity
            android:name="app.wristkey.WiFiTransferActivity"
            android:label="Wi-Fi transfer" />

        <activity
            android:name="app.wristkey.SendActivity"
            android:label="Send" />

        <activity
            android:name="app.wristkey.ReceiveActivity"
            android:label="Receive" />

        <activity
            android:name="app.wristkey.ManualEntryActivity"
            android:label="Add account" />

        <activity
            android:name="app.wristkey.ExportActivity"
            android:label="Add account" />

        <activity
            android:name="app.wristkey.AboutActivity"
            android:label="About" />

        <activity
            android:name="app.wristkey.DonateActivity"
            android:label="About" />

        <activity
            android:name="app.wristkey.SettingsActivity"
            android:label="Settings" />

    </application>

</manifest>